package com.wsy.algorithm.niuke;

public class Solution3 {
    public int characterReplacement(String s, int k) {
        //从该字符出现的第一次开始进行算,每次替换都k--,到末尾k>0,则直接+k,
        //"ABAB" ->
      return 0;
    }
}
